Add a new event:

<%= semantic_form_for @event do |f| %>
	<%= f.input :title %>
	<%= f.input :date, :as => :date_picker  %>
	<%= f.input :place %>
	<%= f.input :description %>
	<%= f.submit "Create Event", :class => "submit-button" %>
<% end %>
